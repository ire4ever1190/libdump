# Lib dump

[docs](https://ire4ever1190.github.io/libdump/stable)

Small dumping groud of utility functions I reimplemented many times but don't think are good enough to raise a PR to the stdlib.
Idea is to have them here so I test them better and stop reimplementing them multiple times
